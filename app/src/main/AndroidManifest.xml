<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mohamed.travelb">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".UserAreaActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".PlansActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".RecyclerViewAdapter" />
        <activity android:name=".foundSearch" />
        <activity android:name=".addplan" />
        <activity android:name=".ContactDtails" />
        <activity android:name=".Send_message" />
        <activity android:name=".send_message" />
        <activity android:name=".MessageDtails" />
        <activity android:name=".remove" />
        <activity android:name=".messageMenu"></activity>
    </application>

</manifest>